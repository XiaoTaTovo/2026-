# 2026 全国大学生电子设计竞赛 H 题

- `master/`：MSPM0G3507 主控 CCS 工程，默认且当前唯一可用模式为正式要求 2（B2）。
- `vision/maxicam/`：球位视觉，接口冻结后再接入主控。
- `vision/picture/`：独立图传链路（要求 1）。

主控的电机执行链只使用 `TB6612 + 双轮编码器速度环`。视觉目录独立开发，本次
主控纯化不修改其中内容。构建、接线、安全门、调参字段和首轮上板顺序见
[`master/README.md`](master/README.md)。
